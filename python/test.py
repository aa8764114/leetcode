# %%
def check_engine():
    # 測試 Python 的內建函數運作是否正常
    items = [1, 2, 3, 4, 5]
    squared = [x**2 for x in items]
    print(f"Engine Test: {squared}")
    print("Ready to crush Google's algorithms!")

if __name__ == "__main__":
    check_engine()